# Decision Request — Pilot 1 account cohort classification at provisioning time (Sprint 1.3 phase B)

**Date:** 2026-09-08 · **Raised by:** Claude (developer seat) · **Ratifier:** Evans (+ Engineering Lead + CDM owner) · **Trigger:** hard-floor item 6 — two merged, Codex-reviewed artifacts disagree on who may write `accounts.cohort_classification`, and every reconciliation touches a DB role privilege or a SECURITY DEFINER pattern.

> This file is the **source-first question** for the two-pass Codex consult. It deliberately contains NO recommendation and NO option framing from Claude. Claude's separate recommendation lives in `Decision-Request-Pilot-1-Account-Classification-Provisioning-2026-09-08.CLAUDE.md` and is supplied to Codex only at Pass 2.

## 1. The question

**How must an account created through the ordinary Identity registration paths (`createAccount` in `src/modules/identity/internal/services/account-service.ts` → `src/modules/identity/internal/repositories/account-repo.ts`, executed under `identity_service_role`) receive its `cohort_classification`, given the conflicting ratified and merged artifacts below?**

Sub-questions the ratifier's answer must settle:

1. May `identity_service_role` write `accounts.cohort_classification` at all, and if so, may it only write a value derived from `account_type` rather than a caller-supplied one?
2. Must the classification be present in the same `INSERT` statement (atomic), or is a same-transaction privileged follow-up write acceptable, or is deferred operator remediation acceptable?
3. What classification does a `patient` / `delegate` created by ordinary registration on the Pilot 1 substrate receive (`participant` per migration 080's own note, or `unclassified` pending privileged provisioning per migration 083)?

## 2. The conflicting sources (verbatim pointers; all merged on `telecheck-app` main)

### 2a. `docs/PII_SCREENING_AND_LOG_REDACTION_SPEC.md` (ratified 2026-08-30 under Path α) — §Three-state cohort classification / §Cohort-classification integrity

- "**Provisioning contract:** every account-creation code path MUST specify the classification atomically in the same INSERT. Attempting an INSERT without the classification column value fails at the NOT NULL constraint."
- "**Provisioning (atomicity, not post-hoc):** account creation and `cohort_classification` assignment MUST be in the SAME `INSERT INTO accounts (...) VALUES (...)` statement. There is no post-hoc classification-write pattern."
- "**Provisioning post-verification (backstop, not the only line of defense):** the provisioning helper MAY additionally run `verify-pilot-1-baseline.sh` as a same-transaction post-check … If the verifier detects any `unclassified` row, the provisioning transaction rolls back."
- CI test 4: "every account-creation path in Pilot-1 baseline-seed + participant-provisioning flows MUST atomically write `cohort_classification` as part of the same INSERT."
- Three-state definitions: `participant` = "a Pilot-1 participant (patient or delegate) whose data is purged by env-purge"; `baseline` = staff types "OR a patient/delegate seeded for baseline test purposes"; `unclassified` = "a data-model defect: either provisioning failed to assign a classification, or a manual database write bypassed the provisioning path."

### 2b. `migrations/080_pilot_1_cohort_classification.sql` (Sprint 1.3 phase A, merged `a54bb91`, Codex R3 APPROVE)

- Step 3 replaced NOT-NULL-without-DEFAULT by `DEFAULT 'unclassified'` because `createAccount` "does not supply the column, so the no-DEFAULT form … failed *totally*, breaking every account-creation path".
- "⚠️ CONSEQUENCE FOR SPRINT 1.3 PHASE B: because `createAccount` does not yet classify, every account it creates lands 'unclassified', so the preflight gate will refuse until each is remediated. That is fail-closed but operationally noisy. **Phase B must give `createAccount` an explicit classification argument** — applying this migration's own backfill mapping (clinician / tenant_admin / platform_admin / service -> baseline; patient / delegate created during the pilot -> participant) with an override for baseline test fixtures … That is a contract-completion task and is deliberately NOT decided here."

### 2c. `migrations/083_identity_account_control_field_privileges.sql` (Identity bootstrap package, merged later than 080, Codex-reviewed)

```sql
REVOKE INSERT, UPDATE ON public.accounts, public.sessions FROM telecheck_app_role;
GRANT INSERT (
    account_id, tenant_id, phone_e164, email, first_name, last_name,
    date_of_birth, gender, national_id, country_of_residence,
    country_of_care, locale
) ON public.accounts TO identity_service_role;
GRANT UPDATE (status, activated_at) ON public.accounts TO identity_service_role;
-- account_type defaults to patient; cohort_classification defaults to
-- unclassified. Both remain governed by separate privileged provisioning.
```

### 2d. `tests/integration/identity-ordinary-role.test.ts` — `describe('trusted Identity account control fields')` → `it('allows Identity registration and activation but forbids privileged role/cohort writes')`

Under `SET SESSION AUTHORIZATION identity_service_role` the test asserts that (i) `UPDATE accounts SET cohort_classification='baseline'` is **rejected**, (ii) an `INSERT … cohort_classification … 'baseline'` is **rejected**, and (iii) an ordinary registration row reads back as `{ account_type: 'patient', cohort_classification: 'unclassified', status: 'active' }`.

### 2e. `src/modules/identity/internal/repositories/account-repo.ts` — `createAccount`

- "Ordinary registration relies on the database's patient default and cannot write governed role/cohort fields. Nonpatient provisioning is deliberately a privileged path requiring INSERT(account_type)." The INSERT names `account_type` only when the caller supplies a non-patient type; it never names `cohort_classification`.
- Call sites: `handlers/registration.ts` (phone OTP registration), `handlers/email-pin-auth.ts` (email+PIN registration), `services/account-service.ts`; staff accounts are created through the separate staff-enrollment path (`handlers/staff-enrollment.ts`, migration 098).

### 2f. Enforcement surfaces already merged

- `scripts/verify-pilot-1-baseline.sh` refuses (exit 1) on any `unclassified` row; runs as Day-0 checklist item, env-purge preflight (both modes) and provisioning post-hook.
- `scripts/pilot-1-marker-remediation.sh` (Sprint 1.3 phase B, not yet merged) is the only authorized classification route for `unclassified` rows; each call emits `pilot_1.cohort_classification{accountId, classifiedAs, actor, reason}`.
- Migration 031 `_session_actor_context` + SECURITY DEFINER wrapper pattern (`*_wrapper_owner` roles in migrations 035/036/042/044/047/056/076/079) is the codebase's canonical way to let an unprivileged role perform a constrained privileged write.

## 3. Constraints the answer must respect

- **I-003 / I-027**: every classification write that is not part of the creating INSERT must be audited with tenant attribution; bare suppression forbidden.
- **I-023**: the identity trust boundary (migration 081/083) was designed so `identity_service_role` cannot write governed fields; any change must keep RLS + column-privilege + app-layer enforcement coherent.
- **Pilot 1 substrate semantics**: env-purge deletes `participant` rows only; `unclassified` rows block purge; `baseline` rows survive. Pilot 2 (real PHI) does not run env-purge.
- **Spec ratification leads implementation**: Claude may implement only a ratified sub-decision; the ratifier picks.

## 4. What Claude is asking Codex (Pass 1)

Read ONLY this file and the cited sources on `telecheck-app` main. Produce an independent recommendation: enumerate the viable provisioning designs yourself, recommend one, give the strongest argument for and against each, and name any deal-breaker (invariant violation, trust-boundary regression, spec contradiction) on any design. Do not consult Claude's recommendation file.

## 4a. Codex Pass 1 — source-first independent (verbatim, 2026-09-08)

# Codex Adversarial Review

Target: branch diff against main
Verdict: approve

recommend-pass-1 — Recommend a constrained SECURITY DEFINER creation function that explicitly inserts the Pilot-1 classification while identity_service_role retains no direct write privilege over governed fields. This is an architectural recommendation, not branch approval; the schema requires the outer label 'approve'.

No material findings.

Next steps:
- RECOMMENDED DESIGN: Ordinary Pilot-1 registration calls a narrow database creation function through createAccount, using the existing transaction. The function explicitly inserts account_type='patient' and cohort_classification='participant' together with the profile fields, then returns the account. Accept no caller-selected classification, account type, baseline-fixture override, or writable session flag that changes this policy. Baseline patient/delegate fixtures and staff creation remain separately authorized provisioning operations. A separately authorized Pilot-1 delegate creator should likewise insert 'participant'; the supplied ordinary registration handlers create patients.
- AUTHORITY AND TRANSACTION CONTRACT: Give the function a dedicated NOLOGIN, NOSUPERUSER, NOBYPASSRLS owner with only necessary privileges; grant narrowly scoped EXECUTE without runtime membership in that owner. Revoke PUBLIC execution, protect function/schema ownership, pin a safe search_path, and qualify referenced objects. Preserve FORCE RLS and validate the tenant against the bound context. Reuse the caller's transaction and account-service audit/outbox callback; preserve verification, activation, credential/session creation, uniqueness errors, and idempotency behavior. Do not introduce another connection or an independently committed creation step. Migration 031 supplies the restricted-execution pattern, not a reason to grant actor-binding authority or require an existing authenticated account during registration.
- RATIONALE: Classification controls deletion versus preservation, so authority must cover the value chosen, not merely whether application SQL names the column. The spec requires explicit same-INSERT assignment (docs/PII_SCREENING_AND_LOG_REDACTION_SPEC.md:385,492–494); migration 083 denies Identity direct governed-field writes (migrations/083_identity_account_control_field_privileges.sql:16–23). A privileged creation function satisfies both security objectives. The integration fits account-repo.ts:261–313 and account-service.ts:105–127; both registration handlers already supply their transaction (registration.ts:299–315; email-pin-auth.ts:324–338).
- DESIGN A — CONSTRAINED CREATION FUNCTION; RECOMMENDED. FOR: Preserves direct column denial and literal same-INSERT classification, with one auditable place defining the ordinary-registration policy. AGAINST: Adds a privileged execution surface whose ownership, grants, tenant handling, and deployment compatibility require careful review. DEAL-BREAKERS: Caller-controlled baseline selection, unrestricted nonpatient creation, RLS bypass, runtime SET ROLE access to the owner, or separation from existing transactional evidence. SECURITY DEFINER alone establishes none of these protections.
- DESIGN B — DIRECT INSERT COLUMN GRANT WITH DATABASE-ENFORCED VALUE RESTRICTION; CONDITIONALLY VIABLE. Identity explicitly supplies 'participant', while an INSERT-specific database restriction independently forbids every unauthorized value and preserves tenant isolation; UPDATE remains denied. FOR: Keeps explicit INSERTs and requires little repository restructuring. AGAINST: Changes the column-privilege boundary and makes correctness depend on composing role-sensitive restrictions with existing RLS and privileged fixture/staff paths. DEAL-BREAKER: A grant plus TypeScript derivation alone lets arbitrary Identity SQL select 'baseline', silently preserving participant data. A general patient-to-participant CHECK would also wrongly prohibit legitimate baseline patients. This option requires explicit ratification of the revised privilege contract and tests.
- DESIGN C — DATABASE INSERT-TIME DERIVATION, SUCH AS A BEFORE INSERT TRIGGER; CONDITIONALLY VIABLE AFTER SPEC AMENDMENT. FOR: Can classify the newly inserted row without granting Identity direct column writes and can cover multiple insertion callers centrally. AGAINST: Classification becomes implicit; caller/provisioning context and privileged fixture exceptions must be trustworthy. DEAL-BREAKER UNDER THE CURRENT SPEC: Allowing an omitted classification to succeed contradicts the explicit-column and omitted-INSERT-rejection requirements at PII spec:385,441,494. An account_type-only derivation is insufficient because patients/delegates can belong to either legitimate cohort.
- DESIGN D — DEFAULT 'participant' WITH EXPLICIT PRIVILEGED OVERRIDES; WEAKER CONDITIONAL ALTERNATIVE. FOR: Preserves current ordinary INSERT shape and direct column denial with minimal machinery. AGAINST: A table default affects every writer that omits the column; an omitted staff/fixture classification could become a destructive purge decision, and an omitted ordinary classification becomes indistinguishable from intentional provisioning. DEAL-BREAKERS: Contradicts the current omitted-INSERT-rejection contract, and is unacceptable without independent safeguards for every other writer and trusted Pilot-1 scope. A default of 'baseline' is unacceptable for ordinary Pilot-1 participants.
- DESIGN E — SEPARATE PRIVILEGED PROVISIONER OWNING THE COMPLETE CREATION TRANSACTION; VIABLE WITH A LARGER BOUNDARY CHANGE. FOR: Keeps classification authority outside ordinary Identity SQL and can explicitly insert the correct cohort. AGAINST: Moving registration work to another authority is substantially more invasive than an in-database function; disabling ordinary Pilot-1 signup and preprovisioning accounts instead changes product behavior. DEAL-BREAKERS: Giving ordinary Identity unrestricted provisioner credentials or SET ROLE capability, or independently committing account creation while verification, audit, credentials, or idempotency remain in another transaction. This changes the premise that ordinary registration executes entirely under identity_service_role.
- DESIGN F — INSERT THEN PRIVILEGED UPDATE IN THE SAME TRANSACTION; REQUIRES SPEC AMENDMENT, NOT RECOMMENDED. FOR: Can prevent an unclassified row from becoming externally visible while retaining direct column denial. AGAINST: Adds a classification transition, affected-row checks, and another privileged operation despite an available one-INSERT solution. DEAL-BREAKERS UNDER CURRENT RULES: The spec explicitly forbids post-hoc assignment and reserves classification of unclassified rows to audited remediation (PII spec:391,492,500). Same-transaction execution does not waive those rules or I-003/I-027: the transition needs tenant-attributed audit, and failed classification or evidence must prevent commit.
- DESIGN G — DEFERRED OPERATOR REMEDIATION; RECOVERY ONLY. FOR: Preserves the pinned current behavior, requires a human decision where cohort intent is uncertain, and leaves purge refusing unclassified accounts. AGAINST: Makes successful ordinary registration routinely create the state the spec calls a data-model defect, accumulating operator work before routine or incident purge. DEAL-BREAKER AS THE STEADY-STATE DESIGN: Violates atomic provisioning; purge refusal contains uncertainty but does not fulfill provisioning. Unlogged remediation additionally violates I-003/I-027. Retain this route for existing unclassified rows and exceptional recovery, not normal creation.
- FRAMING CONCERNS: The artifacts cannot all remain unchanged. Migration 080 actually installs DEFAULT 'unclassified' as a compatibility accommodation (lines 115–149); its Phase-B prose is not an implemented provisioning contract. The ordinary-role test combines valuable denial assertions with a positive expectation that an active account remains unclassified (identity-ordinary-role.test.ts:107–130); preserve the former and replace the latter after ratification. Merge order and previous review labels do not settle policy precedence. Cohort reflects provisioning purpose, not account_type alone: the spec expressly permits baseline patient/delegate fixtures. Ratify that ordinary registration on this substrate means participant enrollment; if that premise is false, require trusted provisioning intent or refuse creation rather than guess. Do not silently extend this Pilot-1 policy to other substrates.
- RATIFICATION AND IMPLEMENTATION FOLLOW-THROUGH: Record the chosen authority, Pilot-1 scope, fixture exception, and explicit same-INSERT requirement. For Design A, use a forward migration and coordinate all writer changes before removing DEFAULT 'unclassified'; otherwise the compatibility failure documented in migration 080 returns. Preserve NOT NULL/CHECK and reject unclassified output from normal provisioning. Keep audited remediation for existing rows. Verify both registration routes under the actual Identity role, direct governed-field denial, unauthorized execution and tenant-context rejection, fixture/staff behavior, audit-failure rollback, and concurrent/replayed registration without reclassification. Preserve the spec's omitted/invalid INSERT and purge-canary coverage. If using a transactional verifier backstop, execute its equivalent query on the existing transaction: verify-pilot-1-baseline.sh:68–95 starts separate psql connections and cannot inspect that transaction's uncommitted account. This consultation used only the requested source files; no implementation or tests were executed.

## 4b. Codex Pass 2 — contrast-and-synthesize (verbatim, 2026-09-08)

# Codex Adversarial Review

Target: branch diff against main
Verdict: approve

recommend-pass-2 — retain Design A: constrained SECURITY DEFINER account creation, with separate operator-only remediation. Approval applies to this recommendation for ratification, not implementation or shipping. Claude’s Option B is transactionally atomic when correctly called, but expands existing-row authority and requires substantive contract amendments. No committed branch delta against main was present; this is a source-grounded decision review. Runtime tests were not run.

Findings:
- [high] The shared classifier gives registration authority over legacy accounts (Telecheck_v1_10_PRD_Update/Decision-Request-Pilot-1-Account-Classification-Provisioning-2026-09-08.CLAUDE.md:13-14)
  Claude’s Option B checks only that the target is unclassified and its classification matches account_type. It does not establish that registration created that account in the current transaction. Identity could therefore classify an existing unclassified patient/delegate as participant, including a baseline fixture awaiting operator review. Subsequent purge would delete it. Migration 080 deliberately leaves existing patient/delegate rows unclassified because account_type cannot settle that decision. An audit records the mutation but does not authorize it. The unspecified exception for an 'operator remediation path' also needs a database-enforced distinction; a script name, reason, caller argument, or mutable setting cannot supply that authority.
  Recommendation: Give Identity a creation-only function that inserts patient/participant and cannot modify existing rows. Reserve unclassified-to-classified UPDATE plus audit for a separately authorized operator entry point. Share validation helpers if useful, without sharing caller authority.
- [high] Option B changes more than the meaning of atomicity (Telecheck_v1_10_PRD_Update/Decision-Request-Pilot-1-Account-Classification-Provisioning-2026-09-08.CLAUDE.md:28-37)
  The proposal treats same-transaction substitution as its one contract cost, while also retaining successful classification-omitted INSERTs and the unchanged ordinary-role test. With migration 080’s default and migration 083’s INSERT grants, bypassing the callback can still commit unclassified accounts. The pinned test actually performs raw SQL and positively expects that outcome; it does not exercise registration. The ratified spec independently requires omitted INSERTs to fail and reserves existing-row classification for operator remediation. Claude’s claim that literal same-INSERT requires sacrificing the trust boundary is a false choice: Design A satisfies both. Its initial classification can coexist with the existing same-transaction account-creation audit.
  Recommendation: Retain literal same-INSERT classification, eventually remove DEFAULT unclassified, and replace the pinned test’s positive registration probe while preserving its privilege-denial assertions. If Option B is selected, explicitly ratify the omitted-INSERT and remediation-authority changes too, and specify database enforcement against skipped classification.
- [high] Staff enrollment bypasses the proposed createAccount integration (telecheck-app/migrations/098_identity_staff_enrollment.sql:120-125)
  Migration 098’s identity_enroll_clinician performs its own accounts INSERT without cohort_classification, and identity_staff_owner lacks INSERT privilege on that column. Updating the shared registration repository therefore leaves staff accounts unclassified and blocking the verifier. Removing the default first instead makes staff enrollment fail. Claude mentions staff baseline classification, but does not settle the required function/grant changes or their interaction with enrollment evidence and authority checks at COMMIT. A generic replacement must not bypass those controls.
  Recommendation: Use a forward migration to amend the existing staff function’s INSERT to include baseline and narrowly extend its owner’s INSERT grant. Preserve pending status, membership authorization, immutable enrollment evidence, activation/session guards, and actual-COMMIT authority validation.

Next steps:
- Contrast and decision diff: We agree on database-constrained writes, preserving Identity’s direct governed-field restrictions, transactional failure rollback, audited remediation, and retaining the unclassified purge gate. Claude B corresponds to my Design F. Its strongest advantage is a centralized audited transition; its same-transaction atomicity is real on the wired path. After considering it, my selected design is unchanged, but the synthesis explicitly adopts that transition for operator remediation and permits shared internal validation. Claude missed the creation-function alternative; my Pass 1 needed fuller treatment of bootstrap authority, independent writers, deployment sequencing, and rollback. I reject Claude A’s wider INSERT grant as the fallback.
- Record authority and source precedence: Evans, Engineering Lead, and CDM owner must ratify the exact new creation capability, owner/grants, remediation authority, and amendments under hard-floor item 6. Migration 080’s phase-B prose is a deferred proposal, not implemented authority; its actual backfill does not map existing patients to participant. Migration 083’s direct-write boundary remains. The decision must expressly retain the literal same-INSERT requirement and distinguish initialization from remediation.
- Record Pilot 1 scope: Identify the authorized substrate and tenants, and explicitly ratify ordinary registration there as participant enrollment. Account type alone does not prove cohort membership. This applies to phone OTP and email+PIN registration; both currently call the shared service. Do not infer Pilot 1 scope from a request flag or function name. Pilot 2 receives neither automatic Pilot 1 enrollment semantics nor env-purge authority; resolve its provisioning compatibility before deploying a shared-schema DEFAULT removal.
- Specify Design A precisely: The function performs INSERT ... RETURNING with fixed patient/participant values, normal lifecycle defaults, and no caller-controlled account_type, classification, or fixture override. Preserve nullable phone support for email-only registration and propagate existing uniqueness errors. Invoke it inside the existing transaction, retaining account audit/outbox emission and subsequent credential/session work. Use a narrowly privileged NOLOGIN, NOBYPASSRLS owner, qualified SQL, pinned search_path, bound-tenant validation, revoked PUBLIC EXECUTE, and no runtime membership in the owner. Registration uses the established tenant-bound Identity/system authority; requiring a pre-existing authenticated actor would prevent signup.
- Record the complete writer inventory and fixture exception: Besides both HTTP registration paths, cover migration 098 staff enrollment, staging/baseline seeds, operational fixture scripts, and participant-kit provisioning. I found references to the kit generator, not an implemented tracked generator in the inspected application paths; do not presume integration. Delegates require an explicitly authorized provisioning path that inserts delegate/participant, not an account_type option on ordinary signup. Permit baseline patient/delegate fixtures only through a separately privileged, documented seed/provisioning capability. Preserve existing fixture classifications on retries; never silently repair legacy conflicts by type. 'service' appears in policy prose but is not currently an allowed AccountType/schema value.
- Record remediation and audit rules: Existing unclassified rows remain operator-reviewed. The operator-only transition must lock/recheck the tenant-scoped row, accept only the approved target classes, and atomically write the classification and attributable audit with a reason. Deny ordinary Identity execution, implicit reclassification, and blanket legacy backfills. Define reconciliation after an uncertain COMMIT so retries cannot create duplicate attestations or overwrite a completed decision. Preserve existing creation audit/events; do not present initialization as operator remediation. Record audit-event registration authority explicitly rather than treating the cited P-048 precedent as blanket permission.
- Record deployment and rollback sequencing: Add capabilities first, update every writer and fixture, drain incompatible application versions, reconcile existing unclassified rows through the audited operator route, then remove DEFAULT unclassified while retaining NOT NULL and the three-state CHECK. Keep Pilot 1 readiness gated until the final contract holds. Rollback must preserve classification data and audit history, restore compatible defaults/function bodies before older writers resume, and remove dependent grants/functions before owner roles. Migration 098’s rollback explicitly requires forward repair; migration 080’s rollback drops classification data and is not an acceptable routine rollback for this change.
- Record test amendments: Replace identity-ordinary-role.test.ts’s raw-INSERT success probe with the creation function and expect patient/participant/active; retain all existing forbidden role/cohort INSERT/UPDATE probes. Test omitted classification as 23502 and invalid classification as 23514 using a suitably privileged fixture writer, so privilege failures cannot mask schema behavior. Add real-role coverage for both registration methods, delegates, staff, fixtures and kit provisioning; cross-tenant/forged-context calls; legacy-row and override denial; audit failure rollback; OTP/passcode, PIN/session and idempotency consistency; concurrency; and migration upgrade/rollback compatibility. Keep explicit unclassified defect fixtures and purge refusal tests. Preserve staff authority-revocation-at-COMMIT regression coverage.
- Close the verifier framing gap: verify-pilot-1-baseline.sh opens separate psql connections, so invoking it cannot inspect the creating transaction’s uncommitted account. Any transactional post-check must query through that same transaction client. Keep the external preflight under an appropriately authorized principal that can inspect the entire Pilot 1 substrate; a tenant-filtered zero count is not evidence that every tenant is clear.

## 4c. Convergence summary for the ratifier

| Reviewer | Recommendation | Key rationale |
|---|---|---|
| Claude (`.CLAUDE.md`) | Option B — keep 083's boundary; constrained SECURITY DEFINER **classifier** performing an UPDATE from `unclassified` in the same transaction, shared with operator remediation | preserves the reviewed boundary; audited; same-transaction atomicity |
| Codex Pass 1 | Design A — constrained SECURITY DEFINER **creation** function that performs the INSERT itself with `patient` / `participant`; identity role keeps no column privilege; remediation stays operator-only | literal same-INSERT; authority over the value, not just the column |
| Codex Pass 2 | **Retain Design A**; adopt Claude's audited transition for operator remediation only; reject Claude's fallback (wider INSERT grant) | three HIGH findings on Option B: a shared classifier lets registration classify **legacy** unclassified rows (a baseline fixture awaiting review would then be purged); Option B silently keeps classification-omitted INSERTs succeeding and the pinned test's positive probe; migration 098 staff enrollment inserts its own accounts row and would stay unclassified (needs its own forward migration + narrow grant) |

**Claude's position after Pass 2:** Claude adopts Design A. Pass 2's first finding is decisive — a classifier keyed only on "row is unclassified" cannot distinguish a row created by this registration from a legacy row awaiting operator review, and an audit row records that mistake rather than preventing it. Design A gives the identity path authority over creation only; existing-row classification remains the operator's, exactly as the spec's remediation contract says.

**What the ratifier is asked to ratify (per Pass 2's list):**
1. Authority: a new narrowly-owned SECURITY DEFINER creation function (fixed `patient` / `participant`, no caller-controlled type, classification or fixture override; bound-tenant validation; revoked PUBLIC EXECUTE; no runtime membership in the owner) invoked by `createAccount` inside the existing transaction; migration 083's direct-write denial retained.
2. Pilot 1 scope: ordinary registration (phone OTP and email+PIN) on the Pilot 1 substrate IS participant enrollment; Pilot 2 inherits neither this semantics nor env-purge authority.
3. Writer inventory: migration 098 staff enrollment amended by forward migration to insert `baseline` with a narrow owner grant; delegates only via an explicitly authorized provisioning path; baseline patient/delegate fixtures only via the privileged seed; `'service'` is prose-only (not an AccountType) and is dropped from the mapping.
4. Remediation: existing unclassified rows stay operator-reviewed via `scripts/pilot-1-marker-remediation.sh` (PR feat/pilot-1-3b-cohort-remediation-seed); no ordinary-role execution, no implicit reclassification, no blanket backfill.
5. Sequencing: capabilities first, every writer updated, existing unclassified rows reconciled, then `DEFAULT 'unclassified'` removed while NOT NULL + CHECK stay; rollback preserves classification data and audit history.
6. Test amendments: replace the ordinary-role test's positive raw-INSERT probe with the creation function expecting `patient` / `participant` / `active`; keep every denial assertion; omitted classification tested as 23502 and invalid as 23514 under a privileged fixture writer.
7. Audit-event registration authority for `pilot_1.cohort_classification`, `env.purge.executed`, `env.incident.abandoned` recorded explicitly (not inferred from the P-048 precedent).

## 5. Ratifier decision

_(to be recorded)_
