# Engineering Review Request — Layer 5 backup redaction: recall-first redaction vs. restorable pseudonymisation

**Date:** 2026-09-08 · **Raised by:** Claude (developer seat) · **Trigger:** Codex round 2 on telecheck-app PR #311 (Sprint 1.2c, Layer 5) · **Status:** OPEN — hard-floor item 6 STOP; R3 on the escalated items is BLOCKED until the ratifier decides. In-scope defects from the same round are being closed on the branch in parallel (see §4).

## 1. What was ratified

`docs/PII_SCREENING_AND_LOG_REDACTION_SPEC.md` §Layer 5 — Backup redaction:

> **How:** wrap `pg_dump` output through a redaction pass that catches any regex patterns in dumped rows before the dump hits durable storage. Rejected patterns get replaced with `[REDACTED:PII]` in the dump; original DB rows are separately audit-logged for incident response.
> **Nuance:** this layer degrades Pilot 1 backup fidelity slightly (redactions in the dump), which is acceptable because Pilot 1 has no PHI worth preserving faithfully anyway.

Success metric (§Success metrics): "Layer 5 backup redaction: 100% recall verified against pg_dump adversarial pass." Restorability of the redacted dump into the constrained schema is **not** a stated requirement; fidelity loss is explicitly accepted.

## 2. What PR #311 built, and what Codex found

R1 established that a regex pass over serialized dump text both misses escaped PII and corrupts SQL; the branch now scrubs only inside decoded values (COPY fields with text-format escapes decoded, SQL string literals, JSON via the JSON-aware scanner with the whole library, numeric values matching a pattern replaced by `0`, printable bytea scrubbed as text), refuses `pg_dump` options that bypass the pipe, and publishes atomically. A real `pg_dump → scrub → psql` round-trip test restores into a scratch database.

R2 (head `7ac0238`) returned six HIGH + one MEDIUM. They split into two classes:

**(a) In-scope implementation defects — closeable inline, being closed now on the branch:**
- bundled short options (`-vf/tmp/raw.sql`, `-vFc`, `-vZ9`) pass the prefix-based guard; PostgreSQL parses bundled options → raw unredacted file on disk;
- a quoted identifier containing an apostrophe (`public."o'neil"`) puts the scanner into literal mode and the following COPY block is never recognised;
- a doubled quote inside an E-literal (`E'it''s …'`) ends escape-literal handling early;
- valid JSON **scalar** strings (fully or partly `\u`-escaped) are not routed through the JSON path → either survive or become invalid JSON;
- Layer 3's numeric-preservation exemptions (`time`, `responseTime`, …) still apply in backup mode, where stored JSON has no log-field provenance;
- `--inserts` / `--column-inserts` / `--attribute-inserts` / `--rows-per-insert` are accepted although INSERT-serialised values are not type-aware-screened.

**(b) Findings that require a scope decision — the subject of this ERR:**
- **Restorability into the actual schema.** Replacing matching values with a token (or `0`) violates `accounts` CHECK constraints on phone/email format and UNIQUE constraints (two distinct emails → one token; two matching bigint keys → both `0`), so a full-schema restore fails at PK/UNIQUE creation and dependent FKs cannot be restored. Codex's recommendation: *"Define schema-aware replacements that preserve formats, uniqueness and PK/FK relationships, or fail before publication when unsupported."*
- **bytea handling from column metadata** rather than content shape (a text value that looks like `\x3125551212` is mis-read as binary and passes unscreened; a real bytea with a NUL followed by an email survives).

Both go beyond "a redaction pass … replaced with `[REDACTED:PII]` … fidelity loss acceptable". Format-preserving, uniqueness-preserving, key-consistent replacement is a pseudonymisation engine, not a redaction pass. Column-metadata-driven typing is achievable from the dump itself (the CREATE TABLE statements precede the COPY blocks) and is smaller, but it is still a design change to how Layer 5 decides what a value is.

**Also surfaced (gap, not a Codex finding):** the spec sentence "original DB rows are separately audit-logged for incident response" is not implemented by #311 and its meaning is unclear (which rows? where? that audit would itself hold the PII). It needs a ratifier reading either way.

## 3. Options

### Option A — Spec as written, with dump-derived column typing (recall-first; restore is diagnostic)
- Keep the ratified contract: 100% recall against the library; fidelity loss accepted; a redacted dump is a **diagnostic** artifact, never a recovery path.
- The scrubber learns column types from the dump's own `CREATE TABLE` statements and applies per-type policy in COPY blocks: `bytea` → explicit policy (scrub printable text payloads, otherwise pass through, documented); `json`/`jsonb` → JSON path with **no** Layer 3 numeric exemptions and scalar-string handling; numeric/identity/key columns (PK, UNIQUE, FK-referenced, `serial`/`bigint` ids) → **never** scrubbed (a bare integer matching a phone regex is a false positive in every real schema; keys are preserved so relationships survive); text → whole library.
- Restore guidance: restore into a scratch database with `--data-only`-style loading under `SET CONSTRAINTS ALL DEFERRED` / constraints dropped, documented as diagnostic; the round-trip test covers the **real migrated schema** with constrained `accounts` rows and reports which constraints a redacted dump cannot satisfy (asserting the known set, so a new violation is visible).
- Reject all INSERT-producing `pg_dump` options; parse and reconstruct an explicit option allowlist.
- Reading of "original DB rows are separately audit-logged": interpret as the **scrub manifest** (table, column, row count, pattern ids redacted — no values), not a copy of the rows; amend the sentence.
- Cost: the in-scope fixes plus DDL parsing for types/keys (~1–2 days), spec wording amendment for the manifest reading.

### Option B — Restorable pseudonymisation (format-, uniqueness- and key-preserving)
- Deterministic, keyed replacement per value (HMAC-derived synthetic email / E.164 phone / SSN-shaped tokens, key-consistent across tables), so the dump restores into the real schema with constraints intact.
- Cost: a pseudonymisation engine with its own key management (the HMAC key is itself sensitive: with it, tokens are re-linkable), per-pattern format generators, collision handling, and a full-schema restore suite. Weeks, not days; and it re-opens the residual-risk analysis (pseudonymised data is still personal data under most regimes).
- Deal-breaker check: this is Pilot 2 compliant-substrate territory per the spec's own non-goals ("These layers do NOT replace Pilot 2's compliant substrate").

### Option C — Backups are encrypt-only; Layer 5 redaction withdrawn
- `pg_dump | age` with the option guard and atomic publish; no scrub. Rely on Layer 3 (logs), Layer 4 (vendor egress), incident capture (`pii-scrub --mode log`) and the env-purge for the durable-storage risk; accept that a backup is raw PHI-substrate-grade material protected by encryption and key custody only.
- Cost: smallest; spec §Layer 5 amended to "encryption + access control, no redaction"; the incident-response runbook's capture path keeps its scrub. Deal-breaker check: contradicts the ratified rationale ("durable storage is the highest-consequence leak vector … if backups are ever shared for troubleshooting, they must be clean").

## 4. Claude's recommendation

**Option A.** It is the ratified contract made precise: recall-first, fidelity loss accepted, restore diagnostic. Deriving column types and key columns from the dump's own DDL removes the bytea ambiguity and the key-collision failure without inventing a pseudonymisation engine, and "keys are never scrubbed" is the right call on the merits — a bare integer is not identifying on its own and preserving it keeps the diagnostic dump navigable. The manifest reading of the "audit-logged" sentence keeps the artifact free of the values it exists to remove. Option B is the correct long-term shape for Pilot 2's compliant substrate and should be recorded there, not built inside Pilot 1's synthetic-only boundary. Option C gives up the layer's stated purpose.

**Independent of the decision,** the class-(a) defects were closed on the branch at `0b948de` (Codex R3 in flight) (bundled options, quoted identifiers, E-literal doubled quotes, JSON scalar strings, Layer 3 numeric exemptions off in backup mode, INSERT options rejected). No merge until the ratifier decides.

## 5. Ratifier decision

_Pending — Evans + Engineering Lead + CDM owner. Both recommendations (Claude above; Codex Pass 1 / Pass 2 below) are surfaced side by side in chat per the dual-recommendation process._

### Codex Pass 1 (source-first, independent) — recorded 2026-09-08, source snapshot `0b948de`

```
# Codex Adversarial Review
Target: branch diff against main
Verdict: needs-attention
recommend-pass-1: Recommend (i): encrypted, recall-first diagnostic artifacts, with migration and synthetic reseed as the reset path. Explicitly disclaim constrained-schema restoration and unrestricted sharing. This is a contract recommendation, not implementation approval; Day-0 remains conditional on the requirements below.
No material findings.
Next steps:
- Evidence and scope: Reviewed the permitted sources and main counterparts; final source snapshot was telecheck-app commit 0b948de. No excluded escalation artifact was opened. The spec requires replacement of regex matches, accepts fidelity loss, and prohibits treating these controls as authorization for real-PHI processing (docs/PII_SCREENING_AND_LOG_REDACTION_SPEC.md:326–334, 541–547). The branch runbook explicitly makes restoration diagnostic (docs/BACKUP_REDACTION_RUNBOOK.md:13). That new wording supports the proposed contract but does not itself establish ratifier approval. The PostgreSQL 15/16 CI result is user-supplied evidence; I did not rerun tests.
- Option (i), recommended — Recall-first redaction plus encryption. FOR: Closest to the existing specification and Pilot 1's purge/reseed model; removes recognized identifiers before durable storage without building a general pseudonymisation system. AGAINST: Matching identifiers can lose uniqueness and relationships, making entire tables unrestorable and reducing forensic usefulness. 'Slightly' degraded fidelity understates that possibility. DISQUALIFIER: This cannot be the sole recovery mechanism where exact operational state or historical evidence must survive. Explicitly define its diagnostic purpose, acceptable losses, and separate reset/evidence arrangements.
- Option (ii) — Restorable, format-, uniqueness- and relationship-preserving pseudonymisation, still encrypted. FOR: Enables realistic relational debugging and constrained-schema loading. AGAINST: Substantially expands scope: accounts has phone/email format checks and tenant-scoped uniqueness; migration 078 adds case-insensitive partial email uniqueness and composite account FKs (migrations/012_accounts.sql:180–207; migrations/078_email_pin_auth.sql:64–77, 112–126). Column types alone cannot solve these requirements. Preserving joins also preserves linkability, and successful SQL loading does not establish usable authentication or faithful evidence. DISQUALIFIER: No publication unless all supported transformations preserve the required constraints and relationships, or unsupported cases fail closed. This has the largest implementation and continuing schema-maintenance cost.
- Option (iii) — Encrypt-only backups. FOR: Avoids redaction-induced corruption and offers the strongest basis for faithful recovery with the least transformation code. AGAINST: Retains every missed identifier and accidentally entered real clinical value; encryption does not perform Layer 5's specified removal. It also concentrates access risk in decryption-key custody. DISQUALIFIER: Not acceptable under the current sanitize-before-write contract merely because age encryption succeeds. Requires an explicit contract amendment, approved storage/access/retention arrangements, and acceptance of the retained-data risk. Smaller coding effort does not imply a quicker Day-0 decision.
- Option (iv) — Reproducible synthetic baseline plus a minimal, encrypted diagnostic export instead of a general SQL data dump. FOR: Minimizes retained data and avoids much of the general dump-parser and pseudonymisation burden. AGAINST: Loses arbitrary snapshot investigation and requires a deliberately designed evidence schema and changed capture contract. DISQUALIFIER: Cannot omit the incident or audit evidence required by the runbooks. This is a credible fallback if a bounded Layer 5 implementation cannot be delivered safely.
- Option (v) — Separate encrypted recovery backups and redacted diagnostic exports. FOR: Separates recovery fidelity from diagnostic disclosure requirements. AGAINST: Retains the complete sensitive-data risk of option (iii), while adding two artifact lifecycles, access policies and validation paths. DISQUALIFIER: A redacted companion artifact cannot justify retaining or distributing an otherwise unapproved raw backup. Consider only if recovery requirements genuinely demand it and the additional custody obligations are accepted.
- Ratifier consideration — Privacy classification: The supplied references establish no legal anonymisation determination. Require counsel to assess retained free text, stable identifiers, linkage and any reversible mapping before changing the handling classification of pseudonymised artifacts. Pending that assessment, treat redacted, pseudonymised and encrypted exports as potentially sensitive. Do not rely on volunteer status as an exemption: the incident runbook itself requires counsel confirmation (docs/PILOT_1_INCIDENT_RESPONSE_MINI_RUNBOOK.md:180–191). Regex-library recall is a bounded technical metric, not proof that all personal or clinical information was removed. Synthetic replacement emails or phones may themselves match regexes, so option (ii) needs a verification oracle that distinguishes replacements from surviving source values.
- Ratifier consideration — Evidence and synthetic-only boundaries: Synthetic clinical scenarios do not establish that operator contacts, credentials, configuration or audit history are disposable. Purge expressly preserves baseline accounts and immutable evidence (spec:376–388, 430–438); reseeding does not by itself prove recovery of that history after host loss. Decide those requirements separately. Also resolve 'original DB rows are separately audit-logged' at spec:326 so it cannot authorize copying raw PII into the purge-exempt audit chain. Encrypted intake content remains outside Layer 5's screening visibility; the client-screening remedy or explicit residual-risk acceptance remains necessary (spec:50–59).
- Day-0 requirement — Ratify the bounded contract: Evans, Engineering Lead and CDM owner should approve artifact purpose, supported dump forms/types, accepted information loss, sharing restrictions, success criteria and failure behavior. Record the contract/schema/screener versions and export scope in the manifest without original sensitive values. For option (i), guarantee screening within that declared domain and usable diagnostic framing, while explicitly excluding constrained-schema restoration. Engineering should provide a bounded implementation estimate against the actual schema; these sources do not support a credible calendar estimate.
- Day-0 requirement — Metadata and verification: Derive bytea handling from column metadata, preferably the same dump's CREATE TABLE declarations and COPY column order. The current content-shape branch at src/lib/pii-screener/dump-scrub.ts:208–230 is not a defensible type contract. Reject data-only input without matching metadata; define unsupported-type and opaque-binary behavior explicitly. Omit unsupported content or fail before publication rather than infer ciphertext from non-printability. Metadata resolves decoding, not CHECK/UNIQUE/FK preservation. Retain the scratch round trip as a framing test, then exercise actual migrated-schema dumps with adversarial values and decoded-value survival checks. The existing test covers selected values in two scratch rows, not application constraints (tests/integration/backup-redaction-roundtrip.test.ts:40–47, 100–112). Option (ii) additionally requires successful constrained-schema restoration and relationship assertions; option (iii) requires faithful recovery verification.
- Day-0 requirement — Custody and operational rehearsal: Run the actual wrapper through age encryption and off-VPS authenticated decryption, checking expected sanitized content. Header and size checks alone establish neither decryptability nor redaction completeness (scripts/backup-redacted.sh:103–109). Verify recipient integrity, record recipient identity, keep private keys off-VPS, and document emergency access plus old-key retention/destruction across rotations. Define routine-backup retention separately from incident-log retention, including downloaded copies; the incident GC policy does not automatically govern /home/deploy/backups. Rehearse stage failures and interrupted publication without a usable success artifact.
- Day-0 requirement — Complete the incident/reset gates under every option: Demonstrate capture, manifest-bound purge, atomic rollback, preserved audit/baseline records, reseed and incident closure. The incident runbook explicitly withholds Day-0 until the capture package ships (docs/PILOT_1_INCIDENT_RESPONSE_MINI_RUNBOOK.md:123–157). Reconcile its regex-plus-local-NER capture promise with Layer 5's regex-only scope; do not silently weaken either contract. Retain the existing ingress NER gate and real-data STOP procedure. Choosing a backup contract does not waive those independent startup requirements.
```

### Codex Pass 2 (contrast-and-synthesize)
_pending_
